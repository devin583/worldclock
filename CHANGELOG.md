# 更新日志

本文件记录 WorldClock 的用户可见变化。

## [0.4.0] - 2026-08-12

### 新增

- 新增真正的单时钟与双时钟自适应布局，并根据时钟数量、显示模式和秒数开关匹配紧凑窗口尺寸。
- 新增“显示秒数”选项；默认使用更省电的分钟级显示。
- 新增生产配置/时区模块测试，以及统一的 `npm run check` 质量门禁。
- 新增 Windows 单实例行为；重复启动时显示并聚焦已有窗口。
- 新增经过逐项验证的 portable executable、portable zip、NSIS installer 和 `SHA256SUMS` 发布产物。
- 新增 npm、Cargo、GitHub Actions 的 Dependabot 配置。

### 变更

- 升级到 Node.js 24.19.0、Rust 1.97.1、Tauri CLI 2.11.4 和 Tauri 2.11.5；开发与 CI 版本固定一致。
- Windows 主窗口的关闭操作改为隐藏到托盘；退出应用必须使用托盘“退出”。
- 后台或隐藏状态暂停时钟刷新；关闭秒数时按分钟边界更新，开启秒数时按秒边界更新。
- 移除在线 Google Fonts 依赖，改用本地系统字体，提高离线启动可靠性。
- Windows workflow 拆分为只读构建 job 和仅 tag 可写的发布 job，并固定第三方 Action commit SHA。

### 修复

- 修复通过本地化时间字符串反解析计算 UTC 偏移导致的夏令时边界错误。
- 修复无效的 `Pacific/Sydney` 时区，统一为有效的 `Australia/Sydney`，并对自定义 IANA 时区做有效性检查。
- 修复模拟时钟指针回零时反向旋转、多个独立高频定时器和组件移除后定时器未清理的问题。
- 修复 Windows 高 DPI 下混用物理像素与逻辑像素造成的窗口尺寸错误。
- 修复设置保存非事务化、原生命令错误被吞掉以及开机启动状态可能与配置分叉的问题。
- 修复双数字布局忽略 12 小时制、并把 Glass/Cute 主题错误降级为 Classic/Minimal 的问题。
- 修复设置保存失败时以推荐尺寸代替真实窗口边界回滚，以及失效开机启动路径仍显示为启用的问题。
- 修复配置只做浅合并、任意 JSON 无边界校验和直接截断写入的问题；加入临时文件、备份恢复和字段白名单校验。
- 修复托盘状态同步、窗口锁定、关闭生命周期和重复实例写同一配置的问题。
- 修复设置界面的标签关联、对话框语义、键盘焦点、状态反馈和可见焦点等可访问性缺口。

### 发布工程

- PR、`main` 和 tag 均执行 JavaScript 语法检查、生产模块测试、三个现有前端审计、Rust format、Clippy、Rust tests 和 RustSec 依赖审计。
- 发布 tag 必须与 `package.json` 版本一致；缺失或空产物会使构建直接失败。
- 发布文件保持未签名状态，README 明确记录 Windows SmartScreen 行为与 SHA-256 验证方法。
